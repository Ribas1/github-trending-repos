<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:id="@+id/iv_contributor_avatar"
        android:layout_width="@dimen/details_avatar_size"
        android:layout_height="@dimen/details_avatar_size"
        tools:background="@color/black"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/tv_contributor_username"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="Avatar"
        android:layout_marginTop="@dimen/default_margin"
        style="@style/DetailsTextBigTextStyle"
        app:layout_constraintEnd_toEndOf="@id/iv_contributor_avatar"
        app:layout_constraintStart_toStartOf="@id/iv_contributor_avatar"
        app:layout_constraintTop_toBottomOf="@id/iv_contributor_avatar" />

</androidx.constraintlayout.widget.ConstraintLayout>